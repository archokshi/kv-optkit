global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'kvopt'
    static_configs:
      - targets: ['kvopt:9001']
  - job_name: 'vllm'
    # vLLM may not expose Prometheus metrics by default; keep as placeholder
    static_configs:
      - targets: ['vllm:8000']
